using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestDBBehavior : MonoBehaviour {
	void Start ()
	{	
		/*
		QuestDB questDB = new QuestDB();
		QuestObj quest = questDB.getQuestFromDB("0");
		Debug.Log(quest._id + " quest worked?");
		
		NpcDB mNPC = new NpcDB();
		NPCObj npc = mNPC.getNpcFromDB("0");
		Debug.Log(npc._id + " npc worked?");
		
		ItemDB itemDb = new ItemDB();
		ApparelObj apparel = itemDb.getApparelFromDB("0");
		Debug.Log(apparel._id + " apparel worked?");
		
		PlayerDB playerDB = new PlayerDB();
		PlayerObj player = playerDB.getPlayerFromDB("0");
		Debug.Log(player._id + " player worked?");
		*/
	}
	
}
